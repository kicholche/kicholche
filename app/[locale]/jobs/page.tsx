import Link from "next/link";import {createClient} from "@/lib/supabase/server";
const labels={bn:{title:"চাকরির আপডেট",intro:"সরকারি, বেসরকারি, স্থানীয় ও ইন্টার্নশিপের তথ্য।",back:"← কী চলছে",salary:"বেতনের তথ্য বিজ্ঞপ্তিতে দেখুন"},hi:{title:"नौकरी अपडेट",intro:"सरकारी, निजी, स्थानीय और इंटर्नशिप की जानकारी।",back:"← Kicholche",salary:"वेतन की जानकारी आधिकारिक सूचना में देखें"},en:{title:"Job Updates",intro:"Government, private, local and internship information.",back:"← Kicholche",salary:"Check the official notice for salary details"}} as const;
export default async function JobsPage({params}:{params:Promise<{locale:string}>}){const {locale:raw}=await params;const locale=(["bn","hi","en"].includes(raw)?raw:"bn") as "bn"|"hi"|"en";const t=labels[locale];const s=await createClient();const {data}=await s.from("jobs").select("id,slug,company,location,salary_text,job_translations(title,description,locale)").eq("status","published").order("created_at",{ascending:false});return <main className="placeholder-page"><Link href={"/"+locale}>{t.back}</Link><h1>{t.title}</h1><p>{t.intro}</p><div className="public-list">{(data||[]).map((j:any)=>{const translations=j.job_translations||[];const tr=translations.find((x:any)=>x.locale===locale)||translations.find((x:any)=>x.locale==="bn")||translations[0];return <article key={j.id}><small>{j.company||"Kicholche"} · {j.location||"India"}</small><h2><Link href={"/job?slug="+encodeURIComponent(j.slug)+"&locale="+locale}>{tr?.title||j.slug}</Link></h2><p>{tr?.description||""}</p><b>{j.salary_text||t.salary}</b></article>})}</div></main>}