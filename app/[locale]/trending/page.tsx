import Link from "next/link";import { createClient } from "@/lib/supabase/server";
export default async function TrendingPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;const s=await createClient();const {data}=await s.from("articles").select("id,slug,article_translations(title,locale)").eq("status","published").eq("trending",true).order("publish_at",{ascending:false});return <main className="placeholder-page"><Link href={"/"+locale}>← Kicholche</Link><h1>আজকের ট্রেন্ডিং</h1><p>সবচেয়ে বেশি দেখা ও আলোচিত আপডেট।</p><div className="public-list">{(data||[]).map((a:any,i)=><article key={a.id}><b>#{i+1}</b><h2>{a.article_translations?.find((x:any)=>x.locale===locale)?.title||a.article_translations?.[0]?.title||a.slug}</h2></article>)}</div></main>}