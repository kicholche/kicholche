import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";async function login(f:FormData){"use server";const s=await createClient();const email=String(f.get("email")||"");const password=String(f.get("password")||"");const {error}=await s.auth.signInWithPassword({email,password});if(error)redirect("/login?error=1");redirect("/account");}async function signup(f:FormData){"use server";const s=await createClient();const email=String(f.get("email")||"");const password=String(f.get("password")||"");const name=String(f.get("name")||"");const {error}=await s.auth.signUp({email,password,options:{data:{display_name:name}}});if(error)redirect("/login?error=1");redirect("/login?registered=1");}export default async function Login({searchParams}:{searchParams:Promise<{error?:string;registered?:string}>}){const p=await searchParams;return <main className="placeholder-page"><a href="/bn">← Kicholche</a><h1>Login</h1>{p.error&&<p>Login failed. Email/password check করুন।</p>}{p.registered&&<p>Registration submitted. Email confirmation থাকলে confirm করে login করুন।</p>}<form action={login} className="admin-form"><h2>Sign in</h2><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" required/></label><button className="admin-btn">Login</button></form><form action={signup} className="admin-form"><h2>Create account</h2><label>Name<input name="name"/></label><label>Email<input name="email" type="email" required/></label><label>Password<input name="password" type="password" minLength={6} required/></label><button className="admin-btn light">Register</button></form></main>}
