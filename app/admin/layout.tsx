import AdminNav from "@/components/admin/AdminNav";import { createClient } from "@/lib/supabase/server";import { redirect } from "next/navigation";
export default async function AdminLayout({children}:{children:React.ReactNode}){const supabase=await createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)redirect("/login");const {data:profile}=await supabase.from("profiles").select("role,display_name").eq("id",user.id).maybeSingle();if(!profile||!["ADMIN","MANAGER"].includes(profile.role))redirect("/");return <div className="admin-shell"><AdminNav/><section className="admin-content"><header className="admin-top"><span>{profile.role}</span><b>{profile.display_name||user.email}</b></header>{children}</section></div>}
