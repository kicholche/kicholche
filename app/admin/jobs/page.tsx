import { revalidatePath } from "next/cache";import { createClient } from "@/lib/supabase/server";
async function createJob(f:FormData){"use server";const s=await createClient();const title=String(f.get("title")||"").trim();const slug=String(f.get("slug")||"").trim();if(!title||!slug)return;const {data,error}=await s.from("jobs").insert({slug,job_type:String(f.get("job_type")||"Job"),company:String(f.get("company")||""),location:String(f.get("location")||""),salary_text:String(f.get("salary")||""),status:"draft"}).select("id").single();if(!error&&data)await s.from("job_translations").insert({job_id:data.id,locale:"bn",title,description:String(f.get("description")||"")});revalidatePath("/admin/jobs");}
async function setStatus(f:FormData){"use server";const s=await createClient();await s.from("jobs").update({status:String(f.get("status"))}).eq("id",String(f.get("id")));revalidatePath("/admin/jobs");}
export default async function JobsAdmin(){const s=await createClient();const {data}=await s.from("jobs").select("id,slug,company,location,status,created_at,job_translations(title,locale)").order("created_at",{ascending:false});return <main className="admin-page"><div className="admin-title"><small>RECRUITMENT</small><h1>Jobs</h1><p>চাকরির listing তৈরি ও publication status পরিচালনা করুন।</p></div><form action={createJob} className="admin-form"><h2>নতুন চাকরি</h2><div className="admin-form-grid"><label>Title<input name="title" required/></label><label>Slug<input name="slug" required/></label><label>Type<input name="job_type" defaultValue="Job"/></label><label>Company<input name="company"/></label><label>Location<input name="location"/></label><label>Salary<input name="salary"/></label><label className="wide">Description<textarea name="description"/></label></div><div className="admin-actions"><button className="admin-btn">Create Draft</button></div></form><table className="admin-table"><thead><tr><th>Title</th><th>Company</th><th>Location</th><th>Status</th><th>Actions</th></tr></thead><tbody>{(data||[]).map((j:any)=><tr key={j.id}><td>{j.job_translations?.find((x:any)=>x.locale==="bn")?.title||j.slug}</td><td>{j.company}</td><td>{j.location}</td><td>{j.status}</td><td><form action={setStatus} style={{display:"inline"}}><input type="hidden" name="id" value={j.id}/><select name="status" defaultValue={j.status}><option>draft</option><option>review</option><option>approved</option><option>published</option><option>archived</option></select><button className="admin-btn light">Save</button></form></td></tr>)}</tbody></table></main>}
