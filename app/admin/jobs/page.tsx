import { revalidatePath } from "next/cache";
import { createClient } from "@/lib/supabase/server";
import { translateFields, type Locale } from "@/lib/translation";

async function createJob(f:FormData){"use server";const s=await createClient();const title=String(f.get("title")||"").trim();const slug=String(f.get("slug")||"").trim();const source=String(f.get("source_locale")||"bn") as Locale;const description=String(f.get("description")||"").trim();if(!title||!slug||!["bn","hi","en"].includes(source))return;const t=await translateFields({title,description},source);const {data,error}=await s.from("jobs").insert({slug,original_locale:source,job_type:String(f.get("job_type")||"Job"),company:String(f.get("company")||""),location:String(f.get("location")||""),salary_text:String(f.get("salary")||""),status:"draft"}).select("id").single();if(error||!data)throw new Error(error?.message||"Could not create job");const rows=(Object.keys(t) as Locale[]).map(locale=>({job_id:data.id,locale,title:t[locale].title,description:t[locale].description}));const {error:translationError}=await s.from("job_translations").insert(rows);if(translationError){await s.from("jobs").delete().eq("id",data.id);throw new Error(translationError.message);}revalidatePath("/admin/jobs");}
async function setStatus(f:FormData){"use server";const s=await createClient();await s.from("jobs").update({status:String(f.get("status"))}).eq("id",String(f.get("id")));revalidatePath("/admin/jobs");}
export default async function JobsAdmin(){const s=await createClient();const {data}=await s.from("jobs").select("id,slug,company,location,status,created_at,original_locale,job_translations(title,locale)").order("created_at",{ascending:false});return <main className="admin-page"><div className="admin-title"><small>RECRUITMENT</small><h1>Jobs</h1><p>এক ভাষায় লিখুন—BN / HI / EN স্বয়ংক্রিয়ভাবে তৈরি হবে।</p></div><form action={createJob} className="admin-form"><h2>নতুন চাকরি</h2><div className="admin-form-grid"><label>Source Language<select name="source_locale" defaultValue="bn"><option value="bn">বাংলা</option><option value="hi">हिन्दी</option><option value="en">English</option></select></label><label>Title<input name="title" required/></label><label>Slug<input name="slug" required/></label><label>Type<input name="job_type" defaultValue="Job"/></label><label>Company<input name="company"/></label><label>Location<input name="location"/></label><label>Salary<input name="salary"/></label><label className="wide">Description<textarea name="description"/></label></div><div className="admin-actions"><button className="admin-btn">Create Draft + Auto Translate</button></div></form><table className="admin-table"><thead><tr><th>Title</th><th>Language</th><th>Company</th><th>Location</th><th>Status</th><th>Actions</th></tr></thead><tbody>{(data||[]).map((j:any)=><tr key={j.id}><td>{j.job_translations?.find((x:any)=>x.locale===j.original_locale)?.title||j.slug}</td><td>{j.original_locale}</td><td>{j.company}</td><td>{j.location}</td><td>{j.status}</td><td><form action={setStatus} style={{display:"inline"}}><input type="hidden" name="id" value={j.id}/><select name="status" defaultValue={j.status}><option>draft</option><option>review</option><option>approved</option><option>published</option><option>archived</option></select><button className="admin-btn light">Save</button></form></td></tr>)}</tbody></table></main>}
